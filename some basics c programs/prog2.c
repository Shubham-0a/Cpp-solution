#include<stdio.h>

int main(){

	/* Create a C program with file name “prog2.c”. This program should read an integer from the
	user, store its value in a variable, and print its hexadecimal value.*/
	//declaration of variable num
	int num = 0;

	printf("enter the number \n");

	//take value of num from trhe user
	scanf("%d",&num);

	//print value of number in hexadecimal 
	printf("value of number in hexadecimal value : %f",pi);

	return 0;
}