#include<stdio.h>

int main(){

	/* Write a C program that takes as input: (i) the coordinates of the center (cx, cy) of a circle, (ii)
	its radius, and (iii) another point P = (px, py), and determines whether the point P lies inside
	the circle, on the circle or outside the circle. Your program should print INSIDE CIRCLE,
	OUTSIDE CIRCLE or ON CIRCLE. NOTE: You should not use pow() or sqrt() functions. */

	

	

	return 0;
}